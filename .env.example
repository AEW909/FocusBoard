DATABASE_URL=postgresql://...
DATABASE_URL_UNPOOLED=postgresql://...
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
OPENAI_API_KEY=

# Push notifications (run: node -e "require('web-push').generateVAPIDKeys()" to generate)
NEXT_PUBLIC_VAPID_KEY=
VAPID_PRIVATE_KEY=
# CRON_SECRET is auto-injected by Vercel — no need to set it manually
